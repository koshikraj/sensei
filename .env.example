# Copy to .env.local and fill in from your Supabase project settings.
# Dashboard uses the anon key (read-only via RLS). Routines use the service key.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
